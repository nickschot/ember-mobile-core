import animate from 'dummy/utils/animate';
import { module, test } from 'qunit';

module('Unit | Utility | animate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animate();
  assert.ok(result);
});
